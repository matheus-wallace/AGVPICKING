## 1. Dependência e permissões

- [x] 1.1 Adicionar `com.google.mlkit:barcode-scanning` (distribuição **bundled**, doc §6.3) ao `gradle/libs.versions.toml` e ao `AgvPickVoice/app/build.gradle.kts`, registrando no toml a versão que de fato resolveu — conferir com `./gradlew :app:dependencies` a partir de `AgvPickVoice/`, do mesmo jeito que foi feito com o Vosk. Fixado em **17.3.0** (último publicado no maven do Google; o `maven-metadata.xml` não avança desde 08/2024). Resolve limpo no `debugRuntimeClasspath`, puxando `barcode-scanning-common`, `vision-common`, `vision-interfaces`, `mlkit:common` e — como dependência transitiva do próprio artefato bundled — `play-services-mlkit-barcode-scanning`.
- [x] 1.2 Declarar `android.permission.CAMERA` no `AndroidManifest.xml` (já estava declarada desde a criação do módulo, junto de `uses-feature camera required=false` — nenhuma mudança necessária) e acrescentá-la ao array `PERMISSOES` da `MainActivity` (o comentário atual já reserva o lugar: "`CAMERA` será pedida pela fatia de visão"). Em release ela não é usada pelo app; em debug é o MockDeviceKit espelhando a câmera do celular (design.md - Decisão 10).
- [x] 1.3 Na bancada, descobrir se o MockDeviceKit já devolve `Granted` para `Wearables.checkPermissionStatus(Permission.CAMERA)` ou se o fluxo redireciona para o app Meta AI, e registrar a resposta em design.md - "Verificação da API de câmera" (é o único item marcado como não verificado ali). **Devolve `Granted` direto**, sem redirecionar: o caminho de solicitação (`Wearables.RequestPermissionContract`) existe e está fiado, mas não chegou a ser exercitado em bancada — só com óculos físico.

## 2. Ajustes e recorte — Kotlin puro, testável

- [x] 2.1 Criar `vision/AjustesVisao.kt` no molde de `audio/AjustesAsr.kt`: `data class` com `fatorRecorte` (0,60), `qualidade` (MEDIUM), `fps` (7), `rotacaoGraus` (0) e a lista de formatos, lida de um `ajustes-visao.properties` opcional no `getExternalFilesDir`, com defaults iguais aos valores de produção (design.md - Decisão 9).
- [x] 2.2 Criar `AgvPickVoice/ajustes-visao.properties.exemplo` comentado, como já existe para o ASR.
- [x] 2.3 Criar `vision/RecorteRoi.kt`: função pura que recebe os três planos de uma imagem `YUV_420_888` (bytes, `rowStride`, `pixelStride`, largura, altura) e a fração de recorte, e devolve um `ByteArray` NV21 contendo **apenas** a região central (design.md - Decisão 5). Sem nenhum import de Android — recebe os planos já extraídos. **Divergência**: o plano é um `ByteBuffer`, não um `ByteArray`. Copiar os três planos para o heap antes de recortar faria existir uma segunda cópia do quadro completo, ainda que por um instante — exatamente o que o §4.4/§9.2 diz que não acontece. Lendo direto do buffer do codec, o único quadro completo que existe é o que ele já tinha. `java.nio` é JVM puro, então o teste continua sem Android. **Ajuste em relação ao planejado:** o plano é um `ByteBuffer`, não um `ByteArray`. Copiar os três planos para o heap antes de recortar faria existir uma segunda cópia do quadro completo, ainda que por um instante — exatamente o que o §4.4/§9.2 afirma que não acontece. Lendo direto do buffer do codec, o único frame completo é o que já existia. `java.nio` é JVM puro, então o teste continua sem Android.
- [x] 2.4 Unit test puro-Kotlin de `RecorteRoi`: dimensões de saída corretas para 60%, alinhamento par exigido pelo croma, `pixelStride = 2` (semi-planar) e `pixelStride = 1` (planar) produzindo o mesmo NV21, e recorte de 100% devolvendo a imagem inteira.
- [x] 2.5 Unit test de `AjustesVisao`: arquivo ausente devolve os defaults; arquivo com uma chave só sobrepõe aquela chave; valor inválido não derruba a leitura.

## 3. Decodificador de vídeo

- [x] 3.1 Criar `vision/DecodificadorHevc.kt` portando o parsing de NAL units de `samples/CameraAccess/.../stream/HevcDecoder.kt` (encontrar prefixo, cachear VPS/SPS/PPS, ativar no primeiro keyframe, preferir decodificador de software), mas configurado **sem `Surface`** e com `KEY_COLOR_FORMAT = COLOR_FormatYUV420Flexible` (design.md - Decisão 3). Documentar no KDoc a origem do código e o porquê da divergência.
- [x] 3.2 Entregar cada frame decodificado por callback como `android.media.Image` obtida de `getOutputImage(index)`, com contrato explícito no KDoc: quem recebe **tem que** consumir a imagem de forma síncrona; o `finally` do decodificador fecha a `Image` e libera o buffer do codec logo em seguida (doc §4.4).
- [x] 3.3 Logar de forma inequívoca o caso de `getOutputImage` devolver `null` (design.md - Riscos), com o nome do codec escolhido e o formato de cor negociado — é a falha que, sem log, vira "não decodifica e ninguém sabe por quê".

## 4. Leitor e controlador de stream

- [x] 4.1 Criar `vision/LeitorDeCodigo.kt`: encapsula o `BarcodeScanner` do ML Kit com os formatos de `AjustesVisao` (`CODE_128`, `DATA_MATRIX`, `EAN_13` por padrão — design.md - Decisão 8), recebe o NV21 já recortado via `InputImage.fromByteBuffer` e devolve o conteúdo lido ou nada, num executor de thread única próprio.
- [x] 4.2 Registrar a tentativa de decodificação com os campos que o doc §4.5 pede (timestamp, passo da cascata, método, tempo, resultado), no `Log` de sempre — o logger JSON continua sendo dívida do projeto, não desta fatia (design.md - Non-Goals).
- [x] 4.3 Expor a sessão corrente no `DatSessionController` como `StateFlow<DeviceSession?>` — `null` quando não há sessão viva —, alimentado pelas transições que o controlador já observa (design.md - Decisão 1). Nenhuma mudança de comportamento nos eventos que ele publica.
- [x] 4.4 Criar `vision/ControladorDeVisao.kt`: observa `PickingActor.state` e a sessão do item 4.3; ao entrar em `EscaneandoProduto` com sessão viva e permissão concedida, chama `addCamera(StreamConfiguration(qualidade, fps, compressVideo = true))`, assina `videoStream` antes de `start()` (ordem do sample) e liga o decodificador; ao sair do estado, encerra o stream e faz `close()` no `Camera` (design.md - "Verificação da API de câmera": sem isso o próximo `addCamera` falha).
- [x] 4.5 Ligar o caminho completo no controlador: frame → recorte 60% → liberação da `Image` em `finally` → leitor ML Kit → `PickingActor.send(DecodificacaoConcluida(codigo))`, com **um frame por vez** e descarte dos que chegarem durante uma análise (design.md - Decisão 6), e guarda para publicar no máximo um evento por escaneamento (spec `vision-stream-decode` - Requirement: Código lido no stream é publicado como evento).
- [x] 4.6 Tratar ausência de permissão (Android ou DAT) como no-op silencioso: sem stream, sem evento, sem derrubar o fluxo (design.md - Decisão 11).

## 5. Máquina de estados e fiação

- [x] 5.1 `PickingReducer.kt`: aceitar `DecodificacaoConcluida` em `EscaneandoProduto`, indo direto para `ValidandoContraDados(itemEmAndamento, codigoLido)` (design.md - Decisão 4). `CapturaDisparada` e `DecodificandoProduto` ficam intactos.
- [x] 5.2 Estender `PickingReducerTest`/`PickingActorTest` com a transição nova e com a garantia de que o caminho por foto (`CapturaDisparada` → `DecodificandoProduto` → `DecodificacaoConcluida`) continua funcionando como antes.
- [x] 5.3 `AppContainer.kt`: construir e expor o `ControladorDeVisao` com escopo/dispatcher próprios, seguindo a convenção já usada por `datScope` (design.md - Decisão 7); ler `AjustesVisao` uma vez só, como é feito com `AjustesAsr`.
- [x] 5.4 `MainActivity.kt`: iniciar o controlador de visão junto dos demais no callback de permissões, e pará-lo em `onStop` — a câmera não pode continuar ligada em segundo plano (doc §8), mesma razão pela qual o microfone já é solto ali.
- [x] 5.5 `MockDeviceBootstrap.kt` (só debug): configurar a fonte de imagem do óculos simulado com `services.camera.setCameraFeed(CameraFacing.BACK)` e acrescentar um hook de broadcast por `adb` para alternar para um vídeo por `Uri`, no mesmo estilo dos hooks `DEBUG_*` que já existem ali.

## 6. Verificação

- [x] 6.1 `./gradlew testDebugUnitTest` a partir de `AgvPickVoice/`: os testes novos de `RecorteRoi`, `AjustesVisao` e reducer passam, e nada em `domain/`/`data/`/`audio/` quebra. Registrar a contagem total, como nas fatias anteriores. **78 testes, 0 falhas** (20 novos + 58 existentes). `assembleDebug` também passa; o APK de debug foi de 127 MB para **150 MB** — ver design.md - Riscos, o ML Kit bundled custou bem mais que "alguns MB".
- [x] 6.2 `./gradlew assembleDebug` e `installDebug` no aparelho de desenvolvimento; conferir no logcat que o stream sobe ao entrar em `EscaneandoProduto` (dirigido pelos botões do painel de dev até lá) e que **não** sobe em nenhum outro estado (spec `vision-stream-decode` - Requirement: Câmera ligada apenas no estado de escaneamento). Confirmado em mais de dez execuções dirigidas por `adb`: nenhuma linha de stream em `AguardandoOrdem`, `OrdemCarregada`, `NavegandoParaEndereco` ou `AguardandoCheckDigit`; `Stream de câmera iniciado` sempre no toque que leva a `EscaneandoProduto`, e `encerrado` na saída.
- [x] 6.3 Apontar a câmera traseira do celular para a **caixa física de Loratamed (Cimed), EAN-13 `7896523202204`**, e confirmar que o painel vai de `EscaneandoProduto` para `ValidandoContraDados` com o código lido, sem passar por `DecodificandoProduto`. Esse é o `ean` da primeira linha da ordem `408176` no mock, então a leitura tem que bater exatamente com o que o botão "Decodificação OK" do painel publicaria. Registrar quantas tentativas em quantas leituras. **Confirmado**: `EscaneandoProduto` → `ValidandoContraDados(codigoLido=7896523202204)` sem passar por `DecodificandoProduto`, com um único evento publicado. Melhor caso: 1,9 s da subida do stream à leitura, 2 tentativas. Com o consenso da seção 7 ligado: 5 de 5 execuções corretas, 2 a 3 tentativas cada.
- [ ] 6.4 **Em aberto — a caixa da bancada não tem DataMatrix.** Repetir com o DataMatrix da mesma caixa (serialização da ANVISA), que é o risco alto do doc §15, e registrar a taxa observada. **Atenção ao ler o resultado quando esta tarefa for retomada**: o DataMatrix de medicamento carrega uma cadeia GS1 (`(01)` GTIN-14 + `(21)` série + `(17)` validade + `(10)` lote), não o EAN-13 puro — algo como `010789652320220421...` é leitura **correta**, o parser GS1 do §6.5 é Marco 2. **Por que ficou aberta**: o único código 2D na caixa de Loratamed é um **QR Code** com a bula digital (RDC 885/2024 tornou isso obrigatório em varejo), não um DataMatrix — `QR_CODE` nem está na lista de formatos aceitos (design.md - Decisão 8), então o leitor simplesmente não reconheceu nada nele (2643 tentativas, `resultado=nada`). A serialização SNCM que motivaria um DataMatrix em varejo foi adiada e não chegou a valer; hoje ele só aparece em linha hospitalar/injetáveis. **Consequência para o doc**: o risco "DataMatrix não decodifica em 720p" do §15 pode não se materializar se as caixas da demo forem de varejo — vale confirmar com quem estiver providenciando as caixas físicas (doc §14) antes de assumir esse risco como ativo. Retomar assim que houver uma caixa com DataMatrix (hospitalar) ou uma etiqueta impressa gerada especificamente para isso.
- [x] 6.5 Confirmar o descarte: com o escaneamento rodando por alguns minutos, verificar por `adb shell` que nenhum arquivo de imagem apareceu em `cache`, `files` ou `getExternalFilesDir` do app (spec `vision-stream-decode` - Requirement: Frame completo é descartado antes de qualquer análise). Com o escaneamento rodando: `cache/` tem só o cache do JNA, `files/` só `mwdat_crash_reports` e `profileInstalled`, e o diretório externo só o modelo Vosk. Nenhum arquivo de imagem em lugar nenhum.
- [x] 6.6 Confirmar o teardown: sair de `EscaneandoProduto` por "parar" (voz) e por desligar o óculos simulado (`DEBUG_DESLIGAR_OCULOS`), e verificar no logcat que o stream é encerrado nos dois casos e que um novo escaneamento depois disso ainda consegue abrir a câmera — é o cenário que o `close()` do `Camera` protege. **Verificado nos três caminhos**: leitura publicada (stream encerrado 69 ms depois), perda de Bluetooth (17 ms depois do `STOPPED`) e transversal de emergência, que é o mesmo ramo do reducer que o "parar" por voz. E um segundo escaneamento no mesmo processo abriu a câmera de novo, que é o que o `close()` protege. Achado de bancada: `powerOff` do MockDeviceKit derruba a sessão mas ela **não volta** (timeout de 20 s); o caminho que recupera é `DEBUG_DESPAREAR_OCULOS` + `DEBUG_LIGAR_OCULOS` — limitação do simulador, registrada em design.md.
- [x] 6.7 Anotar em design.md o que a bancada mediu: formato de cor negociado, rotação necessária, tempo médio por frame e taxa de leitura por formato de código. É o insumo da varredura de distância do doc §10.2 na manhã de 18/09. Registrado em design.md - "Verificação em bancada": permissão do DAT, codec e formato de cor negociado, resolução real de 480×640 contra os 504×896 nominais, `stride` de 512, tempos de 17–109 ms por tentativa, as três baterias de leitura e os três caminhos de encerramento do stream. Falta só a taxa por DataMatrix (tarefa 6.4).

## 7. Consenso de leitura — nasceu da bancada

A primeira bateria de leituras produziu falso positivo em 3 de 5 execuções, dois deles EAN-13 com dígito verificador válido (design.md - "Falso positivo de leitura"). Publicar na primeira leitura de um único frame não é seguro, e em separação farmacêutica esse é o erro mais caro que existe.

- [x] 7.1 Criar `vision/ConsensoDeLeitura.kt`: Kotlin puro, exige o mesmo código em N frames consecutivos; valor diferente **reinicia** a contagem em vez de decrementar (design.md - Decisão 12).
- [x] 7.2 Unit test de `ConsensoDeLeitura`, usando como fixture o falso positivo real observado (`7793563202204` contra `7896523202204`): leitura isolada não publica, alternância nunca publica, e o escaneamento novo não herda contagem do anterior.
- [x] 7.3 Acrescentar `confirmacoesDeLeitura` (default 2) ao `AjustesVisao` e ao arquivo de exemplo, com `1` restaurando o comportamento anterior para a calibração do doc §10 poder varrer os dois lados.
- [x] 7.4 Ligar o consenso no `ControladorDeVisao`, reiniciando-o a cada escaneamento, e atualizar a spec `vision-stream-decode` (Requirement: Código lido no stream é publicado como evento).
- [x] 7.5 Corrigir o `E` que o `DecodificadorHevc` deixava no logcat a cada encerramento — callbacks do `MediaCodec` em voo chegando depois do `parar()`. Só é erro quando o decodificador ainda deveria estar vivo; no encerramento vira `D`. Um erro real de codec não pode nascer afogado em ruído.
- [x] 7.6 Refazer a bateria com o consenso ligado e os três formatos: **5 de 5 corretas, nenhum falso positivo, 2 a 3 tentativas por leitura** (antes: 2 corretas, 3 erradas, 1 a 65 tentativas).

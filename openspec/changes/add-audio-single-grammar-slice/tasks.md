## 1. Dependência Vosk e verificação isolada

- [x] 1.1 Adicionar `org.vosk:vosk-android` ao `gradle/libs.versions.toml` e `AgvPickVoice/app/build.gradle.kts` (`implementation`), e confirmar que o artefato resolve via `./gradlew :app:dependencies` a partir de `AgvPickVoice/`. **Coordenada real é `com.alphacephei:vosk-android`** (não `org.vosk`), fixada em **0.3.75** (a busca do Maven Central reporta 0.3.47 como mais recente, mas o `maven-metadata.xml` vai até 0.3.75), puxando `net.java.dev.jna:jna:5.13.0` — resolve limpo no `debugRuntimeClasspath`.
- [x] 1.2 Baixar o menor modelo pt-BR do Vosk (`vosk-model-small-pt`) e embuti-lo em `app/src/main/assets/`, documentando a fonte/versão do modelo num comentário próximo ou num `README` curto do diretório (design.md - Decisão 5). Em `assets/modelo-vosk-pt/` (51 MB), com `PROVENIENCIA.md` e o arquivo `uuid` que o `StorageService` exige.
- [x] 1.3 Confirmar a API real do Vosk antes de integrar. Feito com `javap` sobre o `classes.jar` do `.aar` (mais barato e mais conclusivo que o teste manual previsto originalmente). Três divergências encontradas e registradas em design.md - "Verificação da API do Vosk": grupo Maven, versão mais recente real, e o endpointer embutido que tornou as tarefas 3.1/3.2 desnecessárias.

## 2. Fonte de áudio

- [x] 2.1 Criar a interface `FonteAudio` em `audio/FonteAudio.kt` (`sampleRate: Int`, `fun fluxo(tamanhoJanela: Int): Flow<FloatArray>`), conforme doc §5.2.
- [x] 2.2 Implementar `AudioMicrofoneSimulado` em `audio/AudioMicrofoneSimulado.kt`: captura via `AudioRecord` a 16 kHz, filtro band-pass 300–3400 Hz, downsample para 8 kHz, emitindo o resultado como `Flow<FloatArray>` — mesma degradação de canal do doc §10.1.
- [x] 2.3 Unit test puro-Kotlin (sem Android/Robolectric) para a etapa de band-pass + downsample de `AudioMicrofoneSimulado`, isolando essa lógica numa função testável independente de `AudioRecord` (padrão já usado em `domain/` para manter lógica pura separada de I/O). O teste pegou um defeito real: um único biquad passa-baixa deixava 6 kHz a -19,5 dB, e essa energia viraria aliasing em 2 kHz — corrigido cascateando um segundo estágio.

## 3. Reconhecedor de comando

- [x] 3.1 ~~Criar `audio/EndpointerPorEnergia.kt`~~ **Cancelada**: o Vosk já traz o endpointer, configurável em segundos (design.md - Decisão 2 revisada). Em vez disso, criar `audio/PerfilEndpoint.kt` — enum puro-Kotlin com os três perfis do doc §5.1 (`COMANDO_CURTO` 280 ms, `DIGITOS` 700 ms, `TEXTO_LIVRE` 900 ms) e o valor em segundos que o `setEndpointerDelays` do Vosk espera, para o Marco 2 só precisar trocar de perfil.
- [x] 3.2 ~~Unit test de `EndpointerPorEnergia`~~ **Cancelada** junto com 3.1 — não faz sentido testar código que não existe mais. O endpointer agora é do Vosk e é exercitado na verificação em dispositivo (5.2/5.3).
- [x] 3.3 Criar `audio/ReconhecedorDeComando.kt`: carrega o `Model` Vosk dos assets, mantém uma única thread dedicada (`HandlerThread` ou `newSingleThreadContext`), constrói um único `Recognizer` com a gramática fixa `["parar", "repetir"]` e nunca o recria (design.md - Decisão 1).
- [x] 3.4 Ligar `FonteAudio.fluxo` → `Recognizer.acceptWaveForm` → `getResult()` quando ele devolver `true` (fim de elocução pelo endpointer do Vosk), mapeando o texto reconhecido para `PickingEvent.ComandoParar` / `PickingEvent.ComandoRepetir` e publicando via `PickingActor.send`; texto fora da gramática ou vazio não publica nada (spec `audio-source` - Requirement: Fala fora da gramática não produz evento).
- [x] 3.5 Expor uma função de start/stop em `ReconhecedorDeComando` que a UI/`AppContainer` chama depois de `RECORD_AUDIO` concedida; sem a permissão, o componente não inicia e não publica nenhum evento (design.md - Decisão 6).

## 4. Wiring

- [x] 4.1 `MainActivity.kt`: solicitar `RECORD_AUDIO` em tempo de execução (mesmo padrão de `BLUETOOTH_CONNECT` já usado para a sessão DAT) e, quando concedida, chamar o start do `ReconhecedorDeComando` exposto pelo `AppContainer`.
- [x] 4.2 `AppContainer.kt`: resolver o `TODO(#audio-source-abstraction)`, construir `fonteAudio: FonteAudio` (`AudioMicrofoneSimulado`) e `ReconhecedorDeComando`, carregando o modelo Vosk na própria thread dedicada de áudio já na construção do container (design.md - Decisão 4), sem bloquear o resto da inicialização.
- [x] 4.3 `AndroidManifest.xml`: declarar a permissão `RECORD_AUDIO`. Já estava declarada desde a criação do módulo — nenhuma mudança necessária.

## 5. Verificação

- [x] 5.1 Rodar `./gradlew testDebugUnitTest` a partir de `AgvPickVoice/` e confirmar que os novos testes do band-pass/downsample passam, sem quebrar nada em `domain/`/`data/`. 52 testes, 0 falhas (5 novos + 47 existentes). `assembleDebug` também passa; APK de debug ficou em 127 MB com o modelo e o `libvosk.so`.
- [x] 5.2 Rodar `./gradlew assembleDebug` e `installDebug`, instalar no dispositivo físico, conceder a permissão de microfone quando solicitada, e confirmar visualmente que falar "parar" perto do celular leva o painel de dev para `SessaoPausada` a partir de qualquer estado operacional, exatamente como o botão correspondente já faz. **A primeira tentativa foi inconclusiva** — feita dizendo "confirma", palavra que não está na gramática desta fatia (é do perfil `CONFIRMACAO`, Marco 2), e pelo alto-falante do Mac com fone conectado; nenhum dos dois caminhos exercita o que a tarefa pede. Refeita com voz humana direta depois da instrumentação da seção 6: **3 "parar" em 3 reconhecidos na primeira tentativa**, e o painel foi de `AguardandoOrdem` para `SessaoPausada(estadoAnterior=AguardandoOrdem, motivo=COMANDO_PARAR)`. Ver design.md - "Segunda rodada".
- [x] 5.3 No mesmo app instalado, confirmar que falar "repetir" não muda o estado exibido no painel (comportamento no-op esperado do reducer) e que ruído de fundo / palavras fora da gramática não disparam nenhuma transição. Verificado em duas partes: **(a) no-op a partir de estado operacional** — com o painel em `AguardandoOrdem`, três "repetir" foram reconhecidos e publicados como `ComandoRepetir`, e o estado exibido continuou `AguardandoOrdem`; **(b) ruído e fala fora da gramática** — cerca de 12 minutos de ambiente, mais duas elocuções de "confirma" e conversa a picos de até -29 dBFS, não produziram nenhuma parcial, nenhum evento e nenhuma transição.

## 6. Observabilidade e calibração de bancada

Nasceu da primeira tentativa de 5.2/5.3: o reconhecimento era intermitente e não havia nenhum dado no logcat para separar as hipóteses (design.md - Decisões 6, 7 e 8).

- [x] 6.1 Criar `audio/MedidorDeNivel.kt` — Kotlin puro, RMS e pico em dBFS relatados uma vez por segundo — e logá-lo no loop de escuta. Responde antes de tudo se o microfone está entregando sinal; a linha de base medida (sala silenciosa, `rms=-62,7 dBFS pico=-34,0 dBFS`) ficou registrada em design.md.
- [x] 6.2 Unit test puro-Kotlin de `MedidorDeNivel` (conversão para dBFS, piso no lugar de `-infinito`, RMS de senoide 3,01 dB abaixo do pico, janelamento do intervalo).
- [x] 6.3 Logar a hipótese parcial do decodificador a cada mudança, e logar em `W` a elocução que fecha sem texto final quando havia um parcial — a assinatura de endpoint cedo demais. O caso comum de elocução vazia por silêncio continua sem log, para não inundar o logcat.
- [x] 6.4 Criar `audio/AjustesAsr.kt`: `data class` com os parâmetros de calibração, lida de um `ajustes-asr.properties` opcional no `getExternalFilesDir` (`adb push` + `force-stop`, sem recompilar os 127 MB do APK). Defaults iguais aos valores de produção, arquivo ausente não muda nada. Modelo comentado em `AgvPickVoice/ajustes-asr.properties.exemplo`.
- [x] 6.5 Tornar configuráveis por `AjustesAsr` os quatro suspeitos: `silencioFinalMs` (o `t_end` do endpointer), `cancelamentoDeEco` (**agora desligado por padrão** — design.md - Decisão 8), `ganho` (com clipping antes da escala int16) e `degradarCanal` (bypass do band-pass/decimação, para separar "modelo pequeno" de "canal degradado"). Fiados no `AppContainer` a partir de uma instância única, para que a taxa da `FonteAudio` e a do `Recognizer` não possam divergir.
- [x] 6.6 Rodar `./gradlew testDebugUnitTest` e `installDebug`: 58 testes, 0 falhas (6 novos + 52 existentes); instalado no SM-G780F e verificado no logcat que os ajustes carregam, a captura abre a 16 kHz -> 8 kHz sem AEC e o medidor relata nível.
- [x] 6.7 Com o app instalado, rodar a bateria de voz humana: falar "parar" e "repetir" perto do aparelho e ler no logcat nível, parciais e resultado final. A partir daí, decidir por evidência qual dos suspeitos ajustar — e só então refazer 5.2 e 5.3. **Resultado: 9 de 9 comandos reconhecidos na primeira tentativa, nenhum falso positivo, e nenhum dos quatro suspeitos se confirmou** — os defaults ficam como estão. Medições e conclusão em design.md - "Terceira rodada".
- [x] 5.4 Medir informalmente o tempo entre abrir o app e o reconhecedor ficar pronto para escutar (carregamento do modelo), e comparar com o tempo de subida da sessão DAT observado na mudança anterior — registrar em design.md se o carregamento do modelo atrasar perceptivelmente a inicialização (design.md - Risco 4). **Risco 4 não se confirmou:** o modelo carrega em 665–860 ms (a primeira execução, que inclui copiar os 51 MB dos assets, ficou em 860 ms). A sessão DAT chegou a `STARTED` em `51.415` e o reconhecedor começou a escutar em `51.540` — 125 ms depois, sem disputa perceptível. Nenhuma mudança necessária.
